#!/usr/bin/env ruby

puts "Hello World"

require "bundler/setup"
require_relative "../config/environment"

# Creates a new instance of CLI controller, calls the call method on it, which launches the user experience/interface.

ClimbingCities::CLI.new.call